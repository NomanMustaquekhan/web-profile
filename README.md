# web-profile

> Personal web profile — static site

## What
Simple static portfolio built with HTML and assets.

## How to run locally
Open `index.html` in a browser.

## Deploy
Push to a GitHub repo and enable GitHub Pages (branch `main` / `docs` as desired).